<!doctype html>
<html>
<head>
  <title>GPSE Report Portal</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

<div class="container mt-4">
  <h3>GPSE Reports</h3>
  <table class="table table-bordered mt-3">
    <thead>
      <tr>
        <th>ID</th><th>Project</th><th>Week</th><th>Owner</th><th>Status</th>
      </tr>
    </thead>
    <tbody>
      {% for r in reports %}
      <tr>
        <td>{{ r.report_id }}</td>
        <td>{{ r.project }}</td>
        <td>{{ r.week }}</td>
        <td>{{ r.owner }}</td>
        <td>{{ r.status }}</td>
      </tr>
      {% endfor %}
    </tbody>
  </table>

  <a href="/kpi-library" class="btn btn-primary">Go to KPI Library</a>
</div>

</body>
</html>
